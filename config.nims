# Uncomment the line below to compile release builds (Slower compile time but faster run time)
#--define: release
# Uncomment the line below to disable the database
#--define: noDb
# Uncomment the line below to disable compiling sass
#--define: noCompileSass
